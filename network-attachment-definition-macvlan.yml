apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
        name: macvlan
spec:
        config: '{
                "cniVersion": "0.3.1",
                "name": "cni-macvlan",
                "type": "macvlan",
                "master": "eth0",
                "ipam": {
                        "type": "dhcp"
                }
        }'

